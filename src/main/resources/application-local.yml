spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/teamd
    username: teamd
    password: ${DB_PASSWORD}
app:
  cors:
    allowed-origins: http://localhost:5173
  external:
    kakao-rest-key: ${KAKAO_REST_KEY:stub}
    odsay-key: ${ODSAY_KEY:stub}
    tmap-app-key: ${TMAP_APP_KEY:stub}
