# 비밀 (Secret Manager에 저장, git에 값 커밋 금지)
DB_PASSWORD=
TOKEN_PEPPER=
ORIGIN_ENC_KEY=
KAKAO_REST_KEY=
ODSAY_KEY=
TMAP_APP_KEY=

# 설정값 (비밀 아님, 운영/로컬 환경마다 값만 다름)
DB_URL=
DB_USERNAME=
CORS_ALLOWED_ORIGINS=http://localhost:5173
CORS_ALLOWED_ORIGIN_PATTERNS=

# docker-compose.prod.yml (Traefik) 전용 — 서비스명·도메인 확정 전에는 임시값 사용
API_DOMAIN=
ACME_EMAIL=
